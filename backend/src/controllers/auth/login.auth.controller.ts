import { Request, Response, NextFunction } from 'express';

export function LoginController(
  req: Request,
  res: Response,
  next: NextFunction,
) {
  try {
  } catch (err) {
    console.error(err);
  } finally {
  }
}
